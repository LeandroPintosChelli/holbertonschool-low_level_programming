#include "main.h"
#include <stdio.h>
/**
 * int _abs - check the code
 * @int: absolute value of an integer
 * Return: Always 0.
 */
int _abs(int);
{
	int abs;
	{
	if (abs >= 0)
	return ('abs');
	else
	return ('abs * -1');
}

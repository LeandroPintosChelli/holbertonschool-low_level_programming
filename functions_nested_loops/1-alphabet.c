#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * It prints the alphabet, in lowercase, followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
char az;

for (az = 'a'; az <= 'z'; az++)

_putchar(az);
_putchar('\n');

return (0);
}

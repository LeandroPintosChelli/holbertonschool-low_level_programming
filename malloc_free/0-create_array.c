#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
* *create_array - check the code
* @size: a
* @c: b
* Return: Always 0
*/
char *create_array(unsigned int size, char c)
{
	int size = i;
	int j;
	char *s;

	if (size == 0)
	{
		for (j = 0; j < size; j++)
		s[j] = c;
		return (NULL);
	}
	else
		if (size != 0)
		{
			return (NULL);
		}
}

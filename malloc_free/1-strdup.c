#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
*
*
*/
char *_strdup(char *str)
{
	int s;
	char *c;

	if (str == NULL)
	{
		return (NULL);
	}
	s = strlen(str);
	ic = (char *)malloc(sizeof(char));

	if (*c == NULL)
	{
		for (s = 0; s < str; s++)
		return (NULL);
	}
	return (s);
} 

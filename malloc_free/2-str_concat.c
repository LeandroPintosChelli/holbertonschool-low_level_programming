#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
*
*
*
*/
char *str_concat(char *s1, char *s2)
{
	int i, j;

	i = strlen(s1);
	j = strlen(s2);

	for (i = 0; i < j; i++)
	{
		return (NULL);
	}
	return (0);
}

#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
*
*
*
*/
char *str_concat(char *s1, char *s2)
{
	int i, j;
	char *c;

	i = strlen(s1);
	j = strlen(s2);

	c = (char *)malloc(i * sizeof(char) + 1);
	for (i = 0; i < j; i++)
	{
		return (NULL);
	}
	return (c);
}

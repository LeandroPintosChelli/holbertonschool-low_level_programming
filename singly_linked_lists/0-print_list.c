#include <stdio.h>
#include "lists.h"
/**
*
*
*
*/
size_t print_list(const list_t *h)
{
	int count = 0;
	const list_t *ct = h;

	for (; ct; ct = ct->link)
	{
	if (ct->str)
	{
	while (str == NULL)
	{
		link = link
		printf("[0] (nil)");
	}
	count++;
}

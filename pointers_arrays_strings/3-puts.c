#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - check the code
 * @s: Write a function that returns the length of a string.
 * Return: Always 0.
 */
void _puts(char *str)
{
	puts (str);
}
